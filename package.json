{
  "name": "oxford",
  "version": "0.2.0",
  "private": true,
  "description": "A teacher-facing classroom game library",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node --check dist/catalog.js && node --check dist/app.js && node --check scripts/serve.mjs && node --check scripts/qa.mjs && node --check scripts/qa-teacher.mjs"
  }
}
